{
  "name": "functions",
  "scripts": {
    "build": "echo 'Run \"npm run build\" from the root project level'",
    "start": "echo 'Run \"npm run start\" from the root project level'",
    "deploy": "firebase deploy --only functions",
    "test": "vitest",
    "test:once": "vitest run"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "firebase-functions-test": "^3.0.0",
    "ts-node": "^10.9.1",
    "vitest": "^4.1.9"
  },
  "private": true
}