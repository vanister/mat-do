import { ItemService } from './ItemService';

describe('ItemService', () => {
  describe('WHEN creating a new item', () => {
    test.todo(`should send a POST request with a 'name'`, async () => {});
  });
});
